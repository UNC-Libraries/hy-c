# frozen_string_literal: true

Gem::Specification.new do |s|
  s.name = 'google-analytics-data'
  s.version = '0.999.0'
  s.summary = 'Stub gem — this application uses Matomo; grpc/GA4 dependencies are not needed.'
  s.authors = ['stub']
  s.files = Dir['lib/**/*.rb']
  s.require_paths = ['lib']
end
