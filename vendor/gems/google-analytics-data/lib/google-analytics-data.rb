# frozen_string_literal: true
# Stub — this project uses Matomo for analytics, not Google Analytics.
# Hyrax's GA4 service requires this gem but wraps the actual require in a
# begin/rescue LoadError, so nothing breaks when the real library is absent.
