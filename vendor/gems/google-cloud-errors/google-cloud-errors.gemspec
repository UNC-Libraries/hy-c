# frozen_string_literal: true

Gem::Specification.new do |gem|
  gem.name          = "google-cloud-errors"
  gem.version = '0.999.0'
  gem.summary = 'Stub gem — this application uses Matomo; grpc/GA4 dependencies are not needed.'
  gem.authors = ['stub']
  gem.files = Dir['lib/**/*.rb']
  gem.require_paths = ['lib']
end